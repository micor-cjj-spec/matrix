# 代码与业务文档对齐检查（2026-03-12）

## 检查范围
- GL 凭证模块
- AR/AP 七类单据模块

## 结论
- **已对齐部分**：
  - 凭证状态机、分录校验、过账与冲销、前端操作链路
  - AR/AP 七类单据的统一MVP流转与接口
- **已对齐新增**：
  1. AR/AP 单据差异化字段
  2. AR/AP 单据联动凭证（审核后生成凭证）
  3. AR/AP 列表高级筛选与导出（CSV）
  4. GL 凭证驳回后可编辑边界（凭证头/分录）与统一审批流口径
  5. GL 凭证批量过账（同步）接口
  6. GL 多币种精度与舍入规则（金额/汇率/原币）
  7. AR/AP 审核后自动生成并关联凭证（保留手动补偿接口）
  8. AR/AP 按单据类型的凭证科目映射模板
  9. 单据↔凭证双向回溯接口（凭证反查来源单据）
  9. AR/AP 与凭证双向回溯接口
  10. 凭证CSV导入（前端）
  11. 凭证OCR导入MVP（图片/PDF解析、预览、确认导入）

## 处理原则
- 文档先标记“当前已实现/待对齐”，避免文档超前于代码。
- 后续每个待对齐项落地后同步更新文档与提交说明。
