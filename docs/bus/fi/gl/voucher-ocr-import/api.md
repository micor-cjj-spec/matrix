# 接口说明

## OCR解析接口
- `POST /voucher/import/ocr/parse`
- 输入：上传文件
- 输出：原始文本、凭证头候选、分录候选、引擎信息、警告信息

## OCR确认接口
- `POST /voucher/import/ocr/confirm`
- 输入：凭证头候选 + 分录候选
- 输出：保存后的凭证草稿详情

## 说明
- 解析接口只负责识别和抽取，不直接落正式凭证
- 确认接口会先保存凭证草稿，再按传入分录保存明细
