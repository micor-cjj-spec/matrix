# 业务规则

## 单据规则
- 科目编码必须唯一且符合编码规则
- 科目层级必须满足父子级关系约束

## 校验规则
- 非末级科目不允许直接记账
- 科目方向必须明确
- 启用科目前需保证关键属性完整

## 状态规则
- 草稿态允许编辑和删除
- 已启用科目不允许直接删除
- 已停用科目不可再被新凭证引用

## 联动规则
- 凭证分录必须引用有效科目
- 报表和余额计算依赖科目方向及层级配置
