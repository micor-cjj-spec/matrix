# 流程设计

## 页面流程
1. 页面初始化时，默认查询“本月第一天到今天”。
2. 前端调用 `GET /voucher/summary` 获取汇总结果。
3. 后端按凭证主表筛选并按日期分组聚合。
4. 前端展示汇总卡片、告警提示和按日期统计表。
5. 用户点击“查看凭证”时，按当日条件跳转到凭证列表页。

## 聚合逻辑
- 先按开始/结束日期、状态、摘要关键字筛选凭证。
- 再按 `fdate` 分组。
- 每组统计数量、总金额以及各状态数量。
- 最终按业务日期倒序返回。
