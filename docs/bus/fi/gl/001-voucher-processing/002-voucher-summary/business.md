# 凭证汇总表业务说明

## 1. 业务名称
凭证汇总表

## 2. 业务定位
凭证汇总表不是制单业务，而是面向总账运营的统计分析页面，用于按日期汇总凭证数量、金额和状态分布。

## 3. 业务目标
- 快速识别待审核、待过账的积压情况
- 观察某一时间范围内凭证金额与状态分布
- 支持从汇总结果反查到凭证列表

## 4. 数据来源
后端通过 `BizfiFiVoucherAnalysisServiceImpl.summary` 直接基于凭证主表 `BizfiFiVoucher` 聚合生成结果，不读取分录表。
