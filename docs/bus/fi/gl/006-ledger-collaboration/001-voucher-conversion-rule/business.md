# 凭证折算规则业务说明

## 1. 业务名称
凭证折算规则

## 2. 业务定位
凭证折算规则用于可视化展示系统当前内置的往来单据转凭证映射规则，并检查单据生成覆盖率与待生成量。

## 3. 业务目标
- 让业务看到单据类型与借贷科目的映射关系
- 统计已审核单据、已生成凭证、待生成单据数量
- 暴露规则覆盖率不足和待生成积压情况

## 4. 数据来源
后端通过 `BizfiFiLedgerCollaborationServiceImpl.voucherRules`，基于内置 `RuleSpec` 与 AR/AP 单据实际生成情况计算结果。
