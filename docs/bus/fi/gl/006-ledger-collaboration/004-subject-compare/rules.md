# 业务规则

## 查询规则
- 日期格式必须为 `yyyy-MM-dd`。
- `startDate` 为空默认当月第一天，`endDate` 为空默认当天。
- `accountCode` 为空时对全部科目做对照，传值时按前缀过滤。
- `diffOnly=true` 时仅返回存在差异的科目。

## 对照规则
- 凭证口径来自已过账/已冲销凭证分录。
- 总账口径来自总账分录。
- 只要期初、借方、贷方、期末任一差异非 0，就标记为 `DIFF`。
- 无差异则标记为 `MATCH`。

## 提示规则
- 当前对照页只按科目维度比较，尚未细分组织与账簿维度。
