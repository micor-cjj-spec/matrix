# 流程设计

## 查询流程
1. 前端调用 `GET /ledger-collaboration/offset-vouchers`。
2. 后端按日期范围读取凭证。
3. 先识别对冲凭证，再根据备注里的原凭证ID、`RV-` 编号规则或原凭证备注中的“已冲销到凭证”做配对。
4. 若原凭证和对冲凭证都找到则标记 `PAIRED`，否则标记 `ORPHAN`。
5. 前端展示配对结果、金额差额和告警信息。
