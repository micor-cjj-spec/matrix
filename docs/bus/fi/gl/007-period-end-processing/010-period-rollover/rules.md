# 业务规则

1. 仅 `SUCCESS` 的关账执行记录允许滚动。
2. 关账执行记录的执行后状态必须为 `CLOSED`。
3. 同一关账执行记录只能滚动一次。
4. 组织财务参数必须存在且为 `ENABLED`。
5. 滚动前组织财务参数当前期间必须等于被关账期间。
6. 下一期间已存在时必须为 `OPEN`。
7. 下一期间不存在时自动创建 `OPEN` 会计期间。
8. 滚动成功后组织财务参数当前期间必须等于下一期间。
