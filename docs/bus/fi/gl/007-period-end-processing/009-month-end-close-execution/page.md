# 页面

## 页面位置

`/ledger/month-end-close-workbench`

## 页面改动

- 检查批次留痕列表增加“执行关账”按钮。
- 仅 `APPROVED` 状态批次显示执行按钮。
- 已关账批次显示 `CLOSED` 状态标签。
- 页面新增“关账执行记录”列表。

## 交互

- 点击执行前弹出确认。
- 执行成功后刷新工作台、批次列表、执行记录。
- 执行失败显示后端业务错误。
