# 流程

1. 用户在月结工作台生成检查批次。
2. 批次无阻塞项后提交。
3. 批次审批通过。
4. 用户点击“执行关账”。
5. 系统校验批次状态必须为 `APPROVED`。
6. 系统复跑月结工作台检查。
7. 若实时检查 `canClose=false`，阻止执行。
8. 系统定位组织与期间对应的会计期间档案。
9. 系统调用会计期间关闭能力，将状态改为 `CLOSED`。
10. 系统写入关账执行记录。
11. 系统将检查批次状态更新为 `CLOSED`。
