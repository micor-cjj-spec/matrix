# 业务规则

1. 按组织和期间查询最新记录。
2. 如果存在期间滚动记录，归档状态为 `ROLLED`。
3. 如果存在关账执行记录但无滚动记录，归档状态为 `CLOSED`。
4. 如果最新批次已批准但未关账，归档状态为 `APPROVED_PENDING_CLOSE`。
5. 如果最新批次存在阻塞项，归档状态为 `BLOCKED`。
6. 如果仅有检查批次，归档状态为 `CHECKED`。
7. 没有任何记录时，归档状态为 `NOT_STARTED`。
