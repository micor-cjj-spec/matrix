# 业务规则

## 1. 批次号规则

批次号格式：

`MEC-yyyyMMddHHmmss-组织-期间`

## 2. 保存规则

生成批次时必须重新执行一次月结工作台检查，避免保存前端过期状态。

## 3. 提交规则

- `fblockingCount > 0` 时禁止提交。
- 只有 `DRAFT` 状态允许提交。

## 4. 批准规则

- 只有 `SUBMITTED` 状态允许批准。
- 批准不修改会计期间状态。

