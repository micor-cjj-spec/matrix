# 接口说明

## 查询接口
- `GET /auxiliary-ledger`
- 输入：组织、期间、科目、辅助类型、辅助对象、是否显示明细
- 输出：辅助总账结果、提示信息

## 导出接口
- `GET /auxiliary-ledger/export`
- 输入：与查询接口一致
- 输出：导出文件流

## 说明
- 辅助总账不提供新增、修改、删除类接口
- 查询接口和导出接口使用同一套口径
