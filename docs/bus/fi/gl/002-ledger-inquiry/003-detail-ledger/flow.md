# 流程设计

## 查询流程
1. 前端默认带入当月第一天到当天。
2. 用户输入日期和科目后调用 `GET /ledger/detail-ledger`。
3. 后端先计算开始日期前的期初净额。
4. 后端查询期间内总账分录，按科目、日期、凭证号、分录行顺序逐条遍历。
5. 每处理一条分录，就基于当前科目的历史净额滚动计算余额。
6. 前端展示明细流水、合计卡片和提示信息。

## 与总分类账的差异
- 明细分类账保留每条总账分录，不做凭证层级汇总。
- 总分类账会把同一凭证层级的多条分录合并展示。
