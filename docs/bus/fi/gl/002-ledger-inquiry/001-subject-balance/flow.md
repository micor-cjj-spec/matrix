# 流程设计

## 查询流程
1. 前端默认带入当月第一天到当天。
2. 用户输入开始日期、结束日期、科目编码后调用 `GET /ledger/subject-balance`。
3. 后端先查询开始日期前的总账分录，计算期初净额。
4. 后端再查询期间内总账分录，计算本期借方、本期贷方和分录数。
5. 按科目汇总后生成余额表行，并返回汇总信息和提示信息。

## 余额计算逻辑
- 期初净额 = 开始日期前借方合计 - 开始日期前贷方合计
- 期末净额 = 期初净额 + 本期借方 - 本期贷方
- 金额方向由净额正负换算为 借 / 贷 / 平
