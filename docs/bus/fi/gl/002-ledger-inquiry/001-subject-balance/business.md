# 科目余额表业务说明

## 1. 业务名称
科目余额表

## 2. 业务定位
科目余额表是总账账表查询中的余额类报表，用于按科目展示期初余额、本期借贷发生和期末余额。

## 3. 业务目标
- 快速查看当前期间各科目余额变化
- 支撑总账月结、对账和科目异常排查
- 作为进一步查看总分类账、明细分类账的入口依据

## 4. 数据来源
后端通过 `BizfiFiLedgerQueryServiceImpl.subjectBalance` 直接基于已过账总账分录 `BizfiFiGlEntry` 计算，不读取期间余额快照表。
