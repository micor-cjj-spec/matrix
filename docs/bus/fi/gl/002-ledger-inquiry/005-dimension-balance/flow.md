# 流程设计

## 查询流程
1. 前端默认带入当月第一天到当天。
2. 用户输入日期、科目、维度后调用 `GET /ledger/dimension-balance`。
3. 后端先查询开始日期前分录，按维度计算期初净额。
4. 再查询期间内分录，按维度累计本期借方、本期贷方和分录数。
5. 前端展示维度余额表、汇总卡片和提示信息。

## 当前维度实现
- 当前维度来源为总账分录字段 `fcashflowItem`。
- 未指定维度的分录会被归并到“未指定现金流项目”。
