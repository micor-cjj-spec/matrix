# 流程设计

## 查询流程
1. 前端默认带入当月第一天到当天。
2. 用户输入日期、科目、维度后调用 `GET /ledger/aux-general-ledger`。
3. 后端先按“维度 + 科目”组合计算开始日期前期初净额。
4. 再查询期间内分录，并按“维度 + 科目 + 日期 + 凭证号 + 摘要”聚合到凭证层级。
5. 每个组合再按时间顺序滚动计算余额。
6. 前端展示辅助总账流水、汇总卡片和提示信息。

## 与辅助明细账的差异
- 辅助总账会把同一凭证层级的分录合并。
- 辅助明细账保留分录粒度。
