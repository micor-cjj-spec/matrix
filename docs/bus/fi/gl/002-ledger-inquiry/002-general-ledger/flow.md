# 流程设计

## 查询流程
1. 前端默认带入当月第一天到当天。
2. 用户输入日期和科目后调用 `GET /ledger/general-ledger`。
3. 后端先计算开始日期前的期初净额。
4. 后端再查询期间内总账分录，并按“科目 + 日期 + 凭证号 + 摘要”聚合到凭证层级。
5. 聚合后的每一行继续按时间顺序滚动计算余额。
6. 前端展示流水表、合计卡片和提示信息。

## 与明细分类账的差异
- 总分类账会把同一凭证层级的多条分录合并展示。
- 明细分类账保留分录粒度，不做凭证层级合并。
