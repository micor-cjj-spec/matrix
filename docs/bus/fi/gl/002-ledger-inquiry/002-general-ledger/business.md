# 总分类账业务说明

## 1. 业务名称
总分类账

## 2. 业务定位
总分类账是总账流水类账表，按科目汇总到凭证层级展示当前期间的借贷发生和滚动余额。

## 3. 业务目标
- 查看某科目在期间内的凭证层级发生额
- 观察科目在每张凭证后的滚动余额变化
- 为科目核对和期末检查提供账簿视图

## 4. 数据来源
后端通过 `BizfiFiLedgerQueryServiceImpl.generalLedger` 基于已过账总账分录 `BizfiFiGlEntry` 聚合生成，不直接查询凭证分录表。
