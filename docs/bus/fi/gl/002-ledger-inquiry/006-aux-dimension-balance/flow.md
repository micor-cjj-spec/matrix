# 流程设计

## 查询流程
1. 前端默认带入当月第一天到当天。
2. 用户输入日期、科目、维度后调用 `GET /ledger/aux-dimension-balance`。
3. 后端先查询开始日期前分录，按“维度 + 科目”组合计算期初净额。
4. 再查询期间内分录，按同样组合累计本期借方、本期贷方和分录数。
5. 前端展示组合余额表、汇总卡片和提示信息。

## 组合维度实现
- 当前组合键为 `fcashflowItem + faccountCode`。
- 未指定维度的分录统一归并到“未指定现金流项目”。
