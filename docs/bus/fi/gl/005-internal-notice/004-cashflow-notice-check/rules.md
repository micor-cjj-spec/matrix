# 业务规则

## 勾稽规则
- `period` 使用 `yyyy-MM`，为空时后端回退当前期间。
- `status=ALL` 表示不过滤历史通知状态。
- 勾稽依据是历史通知与当前候选的 `referenceKey` 是否一致。
- 历史通知当前找不到对应候选时，视为问题已自然解除。

## 金额规则
- `snapshotAmount` 取自历史通知快照。
- `currentAmount` 取自最新现金流候选；若问题已解除则为 0。
- 页面不再单独展示改善金额，而是通过快照金额与当前金额对比判断变化。
