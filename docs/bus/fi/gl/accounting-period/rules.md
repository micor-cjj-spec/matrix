# 业务规则

## 单据规则
- 同一账簿下期间编码必须唯一
- 会计期间必须按时间顺序连续维护

## 校验规则
- 开始日期必须小于等于结束日期
- 已结账期间通常不允许新增或修改凭证
- 反结账前需校验后续期间状态

## 状态规则
- 未启用期间不可直接用于业务处理
- 开放期间允许凭证和余额业务处理
- 已结账期间应限制数据变更

## 联动规则
- 凭证新增、审核、过账均需校验期间状态
- 期末处理和结账依赖当前期间处于可处理状态
