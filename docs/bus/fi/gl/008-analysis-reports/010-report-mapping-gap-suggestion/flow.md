# 流程：报表映射缺口治理建议

1. 用户打开企业纳税表。
2. 系统加载分析结果和 `mappingGaps`。
3. 前端基于缺口的报表类型、科目编码、科目名称、映射类型生成治理建议。
4. 用户点击缺口卡片的“维护映射”。
5. 系统跳转报表科目映射页，并携带来源上下文、推荐项目编码和推荐依据。
6. 映射页展示缺口治理上下文与来源建议。
7. 若推荐项目编码能匹配当前模板下的报表项目，新增弹窗自动带入该项目。
8. 用户确认并保存后，系统按既有逻辑回到来源报表复核。
