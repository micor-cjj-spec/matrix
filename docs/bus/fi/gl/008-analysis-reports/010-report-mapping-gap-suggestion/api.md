# API 说明：报表映射缺口治理建议

## 本轮接口变化
- 不新增后端接口。
- 不修改 `mappingGaps` 响应结构。

## 前端路由参数
- `recommendedItemCode`：来源页推断出的推荐报表项目编码。
- `recommendationReason`：来源页推断出的推荐依据或诊断说明。

## 兼容性
- 未携带推荐参数时，映射页继续使用原有本地推断逻辑。
- 推荐项目编码无法在当前模板下匹配报表项目时，仅展示建议依据，不自动填充。
