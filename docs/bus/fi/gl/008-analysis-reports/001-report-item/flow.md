# 流程设计

## 查询流程
1. 前端先加载报表模板下拉。
2. 用户按模板、项目编码、项目名称调用 `GET /report-item/list`。
3. 后端按模板、编码、名称过滤，并按排序、主键升序返回分页结果。
4. 前端展示项目层级、行次、行类型、期间模式和可下钻标志。

## 树查询流程
1. 需要按模板查看整棵项目树时，前端可调用 `GET /report-item/tree`。
2. 后端按模板ID返回同模板下全部项目，并按排序升序返回。
