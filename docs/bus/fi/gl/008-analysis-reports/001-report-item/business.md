# 报表项目业务说明

## 1. 业务名称
报表项目

## 2. 业务定位
报表项目用于维护三大报表的项目定义、层级、行次、行类型和可下钻属性，是报表模板的基础配置数据。

## 3. 业务目标
- 统一维护报表项目主数据
- 为报表科目映射提供项目基础
- 为资产负债表、利润表等报表渲染提供结构化项目树

## 4. 数据来源
前端 `ReportItemView.vue` 调用 `/report-item/*` 接口；后端由 `BizfiFiReportItemController / ServiceImpl` 基于报表项目表完成查询与基础维护。
