# 流程设计

## 查询流程
1. 前端初始化加载业务单元和报表模板。
2. 用户按业务单元、期间、币种、模板、是否显示零值行调用 `GET /balance-sheet`。
3. 页面展示主表行、告警、校验结果和汇总卡片。
4. 用户点击可下钻项目时，再调用 `GET /balance-sheet/drill` 获取科目明细。

## 页面校验口径
- 页面会展示资产总计、负债和所有者权益总计及其差额。
- 若差额接近 0，则页面标记为“已平衡”；否则标记“未平衡”。
