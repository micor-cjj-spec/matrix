# 业务流程

## 缺口识别
1. 用户查询报表或企业纳税表。
2. 后端聚合总账分录。
3. 后端按显式映射、科目默认报表项目和兜底规则解析报表项目。
4. 仍无法解析的科目进入 `mappingGaps`。

## 缺口处理
1. 前端展示未映射科目清单。
2. 用户点击“维护映射”。
3. 系统跳转到报表科目映射页，并带入 `accountCode`。
4. 映射页自动筛选该科目，用户新增或编辑映射。
