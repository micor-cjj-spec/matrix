# 页面说明

## 企业纳税表
- 当 `mappingGaps` 非空时展示“报表科目映射待处理”区域。
- 每条缺口展示科目编码、科目名称、报表类型和模板名称。
- 点击“维护映射”跳转 `/ledger/report-account-map`。

## 报表科目映射页
- 支持读取查询参数 `accountCode`。
- 如果能匹配到会计科目，自动写入会计科目筛选项并查询。
- 如果不能匹配，提示未找到对应会计科目。
