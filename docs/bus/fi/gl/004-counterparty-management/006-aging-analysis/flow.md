# 流程设计

## 查询流程
1. 前端调用 `GET /arap-manage/aging-analysis`。
2. 后端读取指定日期前的往来单据，并扣减已落库核销链接金额。
3. 仅保留可作为源单据且仍有未核销余额的记录。
4. 按往来方分组，累计未核销余额和已核销金额。
5. 按账龄分桶：`0-30 / 31-60 / 61-90 / 90+`。
6. 叠加信用额度和逾期阈值判断风险状态。
7. 前端展示账龄分布和风险结果。
