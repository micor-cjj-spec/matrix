# 流程设计

## 查询流程
1. 前端调用 `GET /arap-manage/account-query`。
2. 后端读取指定日期前的往来单据，并计算已核销/未核销金额。
3. 再按单据类型、状态、仅看未核销、关键字等条件过滤。
4. 前端展示单据明细、汇总卡片和告警信息。

## 关键口径
- 查询结果直接来自单据级数据，而不是核销批次日志。
- `openOnly=true` 时，只保留 `openAmount > 0` 的单据。
