# 流程设计

## 查询流程
1. 前端调用 `GET /arap-manage/writeoff-log`。
2. 后端按往来类型、往来方、方案号、日期范围查询批次日志。
3. 汇总批次数、链接数、总金额。
4. 若传入 `planCode`，额外查询该方案号对应的核销链接明细。
5. 前端展示批次表和明细表。

## 下钻逻辑
- 默认先看批次列表。
- 点击某一批次的“查看明细”后，前端把 `planCode` 带回接口重新查询。
- 此时下方“核销明细”只展示当前方案号对应的链接。
