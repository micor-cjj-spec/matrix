# 业务规则

## 查询规则
- `docTypeRoot` 只支持 `AR / AP`。
- 日期范围作用于日志操作时间。
- `planCode` 为精确匹配。

## 明细规则
- 仅当传入 `planCode` 时，后端才返回 `linkDetails`。
- 批次汇总的 `linkCount` 来自日志表累计，不是现场重新统计链接表。

## 提示规则
- 后端固定返回一条提示：只有自动核销成功执行后才会生成核销日志。
