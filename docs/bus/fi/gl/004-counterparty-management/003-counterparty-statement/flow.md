# 流程设计

## 查询流程
1. 前端调用 `GET /arap-manage/statement`。
2. 后端读取指定日期前的往来单据，并扣减已落库核销链接金额。
3. 过滤掉非对账所需单据后，按单据维度生成对账行。
4. 同时读取最近核销批次日志。
5. 前端展示对账明细和最近批次，并支持跳到核销日志或凭证页。

## 核心口径
- 原额 = 单据金额
- 已核销 = 已落库核销链接金额
- 未核销 = 原额 - 已核销
- 仅看未核销时，只保留 `openAmount > 0` 的单据
