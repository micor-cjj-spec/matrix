# 业务规则

## 查询规则
- `docTypeRoot` 只支持 `AR / AP`。
- `asOfDate` 为空时按当天处理。
- 对账单默认纳入 `SUBMITTED / AUDITED` 的往来单据。

## 状态规则
- `UNWRITTEN`：已核销金额 <= 0
- `FULL`：未核销金额 <= 0
- `PARTIAL`：其余情况

## 提示规则
- 未指定往来方时，会提示当前结果覆盖全部往来方。
- 最近批次只展示最近一段时间内的自动核销日志摘要。
