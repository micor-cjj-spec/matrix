# 接口说明

## 查询接口
- `GET /account-balance-report`
- 输入：组织、期间、科目区间、是否显示零余额、汇总级次
- 输出：余额表结果行、汇总信息、提示信息

## 导出接口
- `GET /account-balance-report/export`
- 输入：与查询接口一致
- 输出：导出文件流

## 说明
- 科目余额表不提供新增、修改、删除类接口
- 查询接口和导出接口使用同一套口径
