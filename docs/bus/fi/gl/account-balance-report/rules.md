# 业务规则

## 查询规则
- 期间不能为空
- 科目区间为空时默认全量查询
- 组织为空时可按默认组织或当前组织口径处理

## 口径规则
- 期初余额、本期发生额、期末余额的计算口径必须一致
- 树形汇总金额必须等于子级汇总结果
- 零余额过滤仅影响展示，不影响原始计算结果

## 输出规则
- 页面展示结果与导出结果必须一致
- 导出字段顺序和页面字段顺序应保持统一
- 若无数据，应返回空结果和提示信息，而不是报错
