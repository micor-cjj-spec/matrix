# 业务规则

## 查询规则
- `ftemplateId`、`fitemId`、`faccountId` 都为精确匹配。
- 列表按 `fupdatetime` 倒序，再按 `fid` 倒序。

## 维护规则
- 页面要求模板、报表项目、会计科目、映射类型必填。
- `feffectiveFrom`、`feffectiveTo` 当前以前端文本形式维护，后端未见额外期间格式校验。
- 当前代码未见唯一性约束校验，允许维护多条映射记录。

## 优先级规则
- 页面文案明确：显式映射优先级高于系统兜底规则。
