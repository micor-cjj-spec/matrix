# 业务规则

## 查询规则
- `fcode`、`fname` 为模糊匹配。
- `fcategory`、`fstatus` 为精确匹配。

## 维护规则
- 页面要求编码、名称、分类、方向、状态必填。
- 前端允许选择上级项目，形成树状层级。
- 当前后端未见专门的唯一性校验和级次校验逻辑，主要按主表 CRUD 处理。

## 排序规则
- 列表按 `fsort` 升序，再按 `fid` 升序。
