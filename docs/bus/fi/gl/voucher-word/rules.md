# 业务规则

## 单据规则
- 凭证字编码必须唯一
- 同一账簿下应避免重复定义语义相同的凭证字

## 校验规则
- 启用前需配置完整的名称和必要编号规则
- 默认凭证字配置应保证唯一性

## 状态规则
- 草稿态允许编辑和删除
- 已启用状态不允许直接删除
- 已停用凭证字不可用于新凭证

## 联动规则
- 凭证新增和自动生成时可根据场景选择凭证字
- 凭证字停用前需校验是否仍被编号和业务规则引用
