# 流程设计

## 单据流程
1. 前端在 `/payable/application` 打开付款申请页。
2. 新建单据时额外维护 `fpayMethod` 和 `fplannedPayDate`。
3. 提交、审核、驳回、删除规则与应付一致。
4. 审核通过后自动生成凭证并回写关联凭证号。

## 代码说明
- 当前付款申请复用 AR/AP 通用单据实现。
- 与普通应付相比，主要差异是付款方式、预计付款日和单据类型不同。
