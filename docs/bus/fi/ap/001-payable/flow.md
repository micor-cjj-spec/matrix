# 流程设计

## 单据流程
1. 前端在 `/payable/manage` 打开 AP 通用单据页。
2. 用户创建或编辑应付单，后端写入 `bizfi_fi_arap_doc`，初始状态为 `DRAFT`。
3. 草稿或驳回单可再次提交，状态转为 `SUBMITTED`。
4. 已提交单审核通过后，状态转为 `AUDITED`，并自动触发生成凭证。
5. 审核驳回后状态转为 `REJECTED`。

## 凭证联动
- `AP` 单据的内置凭证映射为：借 `6001`，贷 `2202`。
- 审核通过后自动创建两条凭证分录，并把凭证号回写到单据。
