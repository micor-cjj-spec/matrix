# 业务规则

## 状态规则
- 新建单据默认 `DRAFT`
- 仅 `DRAFT / REJECTED` 可提交
- 仅 `SUBMITTED` 可审核或驳回
- 仅 `DRAFT` 可删除

## 校验规则
- 单据类型不能为空
- 金额必须大于 0
- 往来方不能为空
- 单据日期为空时后端默认当天

## 凭证规则
- 仅 `AUDITED` 单据可生成凭证
- 若已存在关联凭证，生成凭证接口幂等返回
- 审核动作会自动触发生成凭证

## 风险规则
- 提交和审核前会按往来方触发信用预警检查
- 若命中“超额度硬拦截”或“超逾期硬拦截”，提交/审核会被阻断
