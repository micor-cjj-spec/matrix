# 流程设计

## 单据流程
1. 前端在 `/receivable/manage` 打开 AR 通用单据页。
2. 用户创建或编辑应收单，后端写入 `bizfi_fi_arap_doc`，初始状态为 `DRAFT`。
3. 草稿或驳回单可再次提交，状态转为 `SUBMITTED`。
4. 已提交单审核通过后，状态转为 `AUDITED`，并自动触发生成凭证。
5. 审核驳回后状态转为 `REJECTED`。

## 凭证联动
- `AR` 单据的内置凭证映射为：借 `1122`，贷 `6001`。
- 审核通过后自动创建两条凭证分录，并把凭证号回写到单据。
