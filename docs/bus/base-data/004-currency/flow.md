# 流程设计

## 页面流程
1. 进入 `/currency` 页面。
2. 前端调用 `fetchList()` 加载币种列表。
3. 用户可执行创建、编辑、提交审核、审核通过、驳回、删除。
4. 每次操作成功后前端重新刷新列表。

## 状态流程
- 新建默认为 `DRAFT`
- `DRAFT` 可提交
- `SUBMITTED` 可审核或驳回
- `AUDITED` 不允许再编辑或删除
