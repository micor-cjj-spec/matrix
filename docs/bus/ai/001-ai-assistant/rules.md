# 业务规则

## 页面规则
- 用户消息为空时不允许发送。
- 页面优先展示模型配置状态。
- 无会话时前端会先创建会话再继续后续交互。

## 展示规则
- 消息按会话维度展示。
- 角色区分为 `user` 与 `assistant`。
- 页面可根据配置状态展示真实模型或降级模式。

## 代码边界
- 当前规则来自前端页面与 `ai.js` 接口调用逻辑。
- 本轮未检到后端 AI 实现文件。
