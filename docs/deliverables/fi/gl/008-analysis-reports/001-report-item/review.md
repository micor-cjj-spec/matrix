# 报表项目评审清单

## 1. 一致性检查
- BUS 与 prompt 是否一致
- 模板、项目编码、项目名称、行次、层级、行类型、期间模式、可下钻、排序口径是否一致
- 初始化自动查询、查询、重置、刷新、分页展示是否一致
- 后端已提供但前端未接入的接口边界是否标注清楚

## 2. 已对齐项
- 页面为 `ReportItemView.vue`
- 后端为 `BizfiFiReportItemController / ServiceImpl`
- 接口为 `/report-item/*`
- 当前页面已接入模板加载与分页查询
- 当前页面未接入新增、编辑、删除入口

## 3. 待确认项
- 树查询是否会在后续版本作为前端显式入口开放
- 行类型、期间模式、可下钻枚举是否需要独立文档化

## 4. 后续建议
- 若后续开放写入口，应同步补前端权限、表单校验与测试说明
- 若树结构要在页面显式展示，应同步补交互与接口使用说明
