# 往来通知单勾稽评审清单

## 1. 一致性检查
- BUS 与 prompt 是否一致
- 仍需处理、已自然解除、快照未清、当前未清、warnings 口径是否一致
- 查询、表格展示、跳转到往来对账单并决定 `openOnly=true` 是否一致
- 往来类型、通知状态、统计日期过滤口径是否一致

## 2. 已对齐项
- 页面定位为把历史通知与当前风险扫描结果做对照的勾稽页面
- 接口为 `GET /internal-notice/counterparty/reconcile`
- 页面为 `CounterpartyNoticeCheckView.vue`
- 数据来源为内部通知单表与最新账龄风险候选结果

## 3. 待确认项
- 勾稽状态枚举是否需要独立文档化
- `openOnly=true` 的最终触发规则是否需要更细说明

## 4. 后续建议
- 若后续增加一键关闭通知或批量处理能力，应同步补接口与测试说明
- 若引入预计算勾稽结果，应同步更新业务口径与 SQL 说明
