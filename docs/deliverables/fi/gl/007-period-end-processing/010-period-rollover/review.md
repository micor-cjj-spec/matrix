# 评审交付

## 已覆盖

- 只能基于成功关账执行记录滚动。
- 关账执行记录必须是 `CLOSED` 结果。
- 同一执行记录唯一滚动。
- 校验组织财务参数存在且启用。
- 校验当前期间等于被关账期间。
- 自动创建或复用下一 `OPEN` 期间。
- 更新组织财务参数当前期间。
- 前端隐藏已滚动执行记录的按钮。

## 剩余风险

- 当前未做分布式锁，极端并发下仍依赖唯一索引兜底。
- 当前只实现正常期间推进，不包含反关账或回退。
