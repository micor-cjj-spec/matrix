# 测试提示词

## 后端

1. 未成功关账执行记录滚动失败。
2. 已滚动执行记录重复滚动失败。
3. 组织财务参数不存在时失败。
4. 当前期间不等于被关账期间时失败。
5. 下一期间不存在时自动创建。
6. 下一期间已 `OPEN` 时复用。
7. 下一期间已 `CLOSED` 时失败。
8. 成功后组织财务参数当前期间更新。

## 前端

1. 成功关账执行记录展示“启用下一期间”。
2. 已滚动记录不展示按钮。
3. 滚动成功后刷新四个区域。
