# 测试提示词

## 后端

1. 未批准批次执行关账应失败。
2. 已关账批次重复执行应失败。
3. 实时检查存在阻塞、预警或待确认时应失败。
4. 会计期间不存在时应失败。
5. 会计期间不是 `OPEN` 时应失败。
6. 成功执行后期间状态为 `CLOSED`。
7. 成功执行后写入执行记录。

## 前端

1. 仅 `APPROVED` 批次显示执行按钮。
2. 执行成功后三个区域刷新。
3. 执行记录列表可见。
