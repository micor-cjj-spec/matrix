# 月结工作台测试提示词

## 测试目标

验证关账前检查结果与现有数据状态一致。

## 建议用例

1. 无业务单元参数，接口可返回系统默认期间结果。
2. 会计期间缺失时产生阻塞项。
3. 有未过账凭证时产生凭证过账阻塞项。
4. 总账借贷不平时产生总账平衡阻塞项。
5. 当前期间已关闭时返回 `CLOSED`。
6. 前端页面能展示检查项、步骤和下钻按钮。

## 验收

后端至少通过编译；前端至少通过构建或静态检查。

