# 财务指标接口提示词

## 目标
根据 `docs/bus/fi/gl/008-analysis-reports/002-financial-indicator/` 下现有业务文档，输出财务指标场景的占位版接口提示词。

## 输入上下文
请仅结合以下已存在文档：
- `business.md`
- `manifest.json`

并严格遵守当前代码事实：
- 当前未配置独立路由页面
- 当前未发现后端 controller / service 实现
- 当前未发现可核对的接口定义

## 输出要求
1. 明确写明“当前无正式接口可对齐”
2. 不虚构 URL、请求参数、返回结构
3. 输出后续补齐接口文档前需要落实的最小前提：页面入口、指标查询接口、趋势接口、指标定义接口、权限控制
4. 所有未落地内容统一标记为“待实现 / 待确认”
