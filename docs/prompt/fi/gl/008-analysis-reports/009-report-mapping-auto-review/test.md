# 测试提示词

使用测试账号 `19106026235` / `123456`：

1. 打开企业纳税表。
2. 点击映射缺口“维护映射”。
3. 验证进入报表科目映射页并自动打开新增弹窗。
4. 不在生产数据上创建真实映射时，可直接构造带 `review=reportMappingResolution` 的企业纳税表 URL 验证复核提示。
5. 若在测试环境保存映射，确认保存后自动返回企业纳税表并展示复核结果。
