# 测试提示词

使用测试账号 `19106026235` / `123456` 登录 `https://micor.top/`：

1. 打开企业纳税表。
2. 确认 `mappingGaps` 面板可见。
3. 点击 `1001 - 库存现金` 的“维护映射”。
4. 确认跳转到报表科目映射页，并出现缺口治理上下文。
5. 确认新增映射弹窗自动打开。
6. 确认模板、科目、映射类型预填。
7. 保存或取消后确认页面仍可继续普通维护流程。
8. 若保存测试映射，需要返回企业纳税表重新查询并观察缺口状态。
