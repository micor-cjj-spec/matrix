# 前端开发提示词

在 `matrix-web` 中实现报表映射缺口治理闭环：

1. 企业纳税表点击缺口“维护映射”时，跳转到报表科目映射页，并追加 `mode=resolve`、来源报表路径、期间、币种、业务单元。
2. 报表科目映射页识别治理模式，展示缺口治理上下文面板。
3. 首次进入治理模式时自动打开新增映射弹窗。
4. 新增弹窗预填模板、会计科目、映射类型，并尝试根据账户属性推荐报表项目。
5. 保存成功后保留提示，并提供返回来源报表复核按钮。
6. 不改变既有普通新增、编辑、删除流程。
