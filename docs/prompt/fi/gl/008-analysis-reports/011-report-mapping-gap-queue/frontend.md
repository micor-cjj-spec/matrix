# 前端提示词：报表映射缺口连续治理队列

请在 `matrix-web` 中实现报表映射缺口连续治理队列：

- 修改企业纳税表页面，基于 `mappingGaps` 生成精简队列上下文。
- `开始治理` 和单条缺口维护按钮跳转时携带 `gapQueue` 与 `gapIndex`。
- 修改报表科目映射页，解析队列参数并展示 `队列进度：第 X / N 项`。
- 在映射页提供上一条/下一条缺口导航，切换时保持来源报表参数、推荐编码和建议文案。
- 保持已有来源建议、推荐项目预填、新增映射自动打开、保存后回跳复核逻辑。
- 不要新增生产保存行为。
