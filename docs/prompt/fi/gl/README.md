# 总账域 Prompt 目录

本目录用于沉淀 `fi/gl` 域下基于真实代码整理的开发提示词。

当前已确认有真实代码落点的对象包括：

- `voucher/`：凭证
- `gl-entry/`：总账分录
- `accounting-period/`：会计期间
- `voucher-type/`：凭证字
- `org-finance-config/`：组织财务配置

说明：

1. Prompt 以 `fi-service` 中真实存在的实体、Controller、Service 实现为基础。
2. 当前阶段暂不补 `sql.md`。
3. 后续如继续发现新的真实对象，再按同样方式扩展。
