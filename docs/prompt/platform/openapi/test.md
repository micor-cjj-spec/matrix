# 开放平台测试提示词

## 必测场景

1. 正确签名可以访问已授权 API。
2. 缺少 AppKey、签名错误、时间戳过期分别返回明确错误码。
3. 相同 AppKey 和 Nonce 重放请求被拒绝。
4. 未授权 API、停用应用、过期授权和 IP 白名单不匹配被拒绝。
5. pageSize 被应用/API/系统上限正确收敛。
6. 默认只能查询 POSTED 凭证。
7. 凭证详情和分录查询同样执行状态权限校验。
8. 日志不记录 AppSecret 和完整签名。
9. Gateway 内部 JWT 过滤器不拦截 `/api/open-api/**`，其他内部接口规则不变。
