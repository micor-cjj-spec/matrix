# 需求输入

## 主题
报表映射缺口一键治理闭环。

## 背景
企业纳税表已经能暴露 `mappingGaps`，并能跳转到报表科目映射页定位模板和科目。当前仍缺少从“发现缺口”到“新增映射”再到“回到来源报表复核”的操作闭环。

## 目标
- 从企业纳税表缺口面板进入映射页时，映射页展示治理上下文。
- 映射页能自动打开新增映射弹窗，并预填模板、科目、映射类型。
- 若能按账户属性推断报表项目，则预填推荐报表项目；不能推断时保留人工选择。
- 映射保存成功后提供返回来源报表复核的动作。

## 测试账号
`19106026235` / `123456`
