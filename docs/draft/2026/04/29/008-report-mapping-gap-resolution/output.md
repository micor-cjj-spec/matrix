# 草稿输出

## 本轮范围
- 企业纳税表跳转报表科目映射页时追加 `mode=resolve` 和来源报表参数。
- 报表科目映射页新增“缺口治理上下文”面板。
- 报表科目映射页按路由上下文自动打开新增弹窗。
- 新增弹窗预填模板、科目、映射类型，并尝试推荐报表项目。
- 保存成功后展示“返回来源报表复核”按钮。

## 不在本轮范围
- 不改数据库结构。
- 不新增自动批量映射任务。
- 不改变报表计算口径。
