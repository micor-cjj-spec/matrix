# 报表科目映射缺口治理输出

## 目标能力
- 报表接口返回结构化 `mappingGaps`。
- 企业纳税表将未映射科目显示为待处理清单。
- 点击“维护映射”后进入 `/ledger/report-account-map` 并自动定位对应科目。

## 验收标准
- 企业纳税表接口返回 `mappingGaps` 数组。
- 线上 `1001-库存现金` 缺口可在企业纳税表看到。
- 报表科目映射页通过 `accountCode=1001` 自动筛选。
- 前端构建通过。
