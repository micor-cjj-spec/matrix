# 草稿笔记：报表映射缺口连续治理队列

## 用户故事
作为总账会计，我在企业纳税表看到多个报表映射缺口时，希望从第一个缺口开始连续处理，并在映射页知道当前处理进度，避免来回跳转后丢失上下文。

## 方案取舍
- 使用前端路由参数携带队列摘要，避免新增后端依赖。
- 队列只作为导航和展示上下文，不作为业务保存依据。
- 队列参数按当前企业纳税表接口返回的 `mappingGaps` 顺序生成，保留推荐编码和建议文案。

## 风险
- URL 参数可能变长，因此只携带缺口治理需要的精简字段。
- 如果用户在另一个窗口修改映射，队列展示可能短暂滞后，但返回来源页后会重新查询并复核。
