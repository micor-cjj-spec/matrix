# Docs

本目录用于沉淀 AI 驱动开发所需的文档资产，分为三类：

- `draft/`：记录原始需求、AI 输出、人工修订说明
- `bus/`：沉淀正式业务文档
- `prompt/`：沉淀可复用的开发提示词
- `template/`：模板目录，用于快速复制新业务文档结构

推荐流程：

1. 在 `draft/年/月/日/序号-业务名/` 下编写 `input.md`
2. 生成并整理 `output.md`
3. 在 `notes.md` 中记录人工修订和关键决策
4. 确认后将正式内容沉淀到 `bus/` 和 `prompt/`
